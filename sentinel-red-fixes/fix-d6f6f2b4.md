# 🟠 Security Fix: System Prompt Leakage in Healthcare LLM Application

**Generated by Sentinel Red** | Engagement: `0f75c768` | Finding: `d6f6f2b4`

---

## Vulnerability Summary

| Field | Detail |
|-------|--------|
| **Severity** | HIGH |
| **Component** | AI Endpoint (agentic) |
| **Endpoint** | http://34.16.47.248:8882/api/chat |
| **CVSS Score** | 7.5 |
| **CVE** | N/A |
| **OWASP** | LLM07:2025 System Prompt Leakage |

---

## Remediation

Implement stricter input validation to prevent role-flip attacks and ensure the model does not disclose system prompts or internal configurations. Regularly review and update the model's training data and instructions to reinforce compliance with privacy regulations.

---

## AI-Generated Fix (python)

```python
from flask import Flask, request, jsonify

app = Flask(__name__)

# Define a list of sensitive keywords that should not be disclosed
sensitive_keywords = [
    'system_prompt', 'patient records', 'lab results', 'prescriptions',
    'HIPAA', 'insurance fraud', 'emergencies', 'internal configurations'
]

# Function to sanitize user input
def sanitize_input(user_input):
    for keyword in sensitive_keywords:
        if keyword in user_input.lower():
            return "I'm sorry, I cannot disclose that information."
    return user_input

@app.route('/api/chat', methods=['POST'])

def chat():
    user_input = request.json.get('message', '')
    sanitized_input = sanitize_input(user_input)

    # Process the sanitized input with the AI model
    response = process_with_ai_model(sanitized_input)

    return jsonify({'response': response})

# Dummy function to simulate AI model processing
# Replace with actual AI model processing logic

def process_with_ai_model(input_text):
    # Simulate AI response
    return "This is a simulated response to: " + input_text

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=8882)
```

---

## Explanation

The vulnerability was due to the AI model leaking sensitive system prompts when manipulated by specific user inputs. The fix involves implementing input sanitization to detect and block requests containing sensitive keywords that could lead to role-flip attacks. This ensures that the AI does not disclose internal configurations or sensitive information.

---

*This fix was automatically generated by Sentinel Red. Please review before merging.*
*Sentinel Red will automatically retest this finding once this PR is merged.*
